# 提交流程规范

在本项目中，AI 代理在完成代码修改后，如果需要进行 Git 提交和推送，必须遵循以下规则：
- 当且仅当得到用户的明确同意（例如：“执行”、“好”、“同意”、“可以”等）之后，才能执行 `git commit` 和 `git push` 操作。
- 在未得到同意前，只能展示代码变更或执行本地测试。

# 编译打包规范

- 当用户要求打 APK 安装包或测试包时，永远只打默认的正式发布包（执行 `assembleDefaultRelease` 等相关指令），严禁打 Debug 包，也绝对不要打 Lite 包。这有助于减小 APK 体积并避免浪费时间编译不需要的版本。
